<x-shape::popover.trigger for="roomy" variant="subtle">Base padding</x-shape::popover.trigger>
<x-shape::popover.trigger for="snug" variant="subtle">Tight padding</x-shape::popover.trigger>

<x-shape::popover name="roomy">
    <x-shape::text size="sm">The default, for something to read.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="snug" padding="tight">
    <x-shape::text size="sm">What the dropdown uses, for a list of rows.</x-shape::text>
</x-shape::popover>
