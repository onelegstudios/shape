<x-shape::popover.trigger for="tight" variant="subtle">Extra small</x-shape::popover.trigger>
<x-shape::popover.trigger for="snug" variant="subtle">Small</x-shape::popover.trigger>
<x-shape::popover.trigger for="roomy" variant="subtle">Base</x-shape::popover.trigger>
<x-shape::popover.trigger for="spacious" variant="subtle">Large</x-shape::popover.trigger>
<x-shape::popover.trigger for="open" variant="subtle">Extra large</x-shape::popover.trigger>

<x-shape::popover name="tight" padding="xs">
    <x-shape::text size="sm">For a menu of nothing but rows.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="snug" padding="sm">
    <x-shape::text size="sm">What the dropdown uses, for a list of rows.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="roomy">
    <x-shape::text size="sm">The default, for something to read.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="spacious" padding="lg">
    <x-shape::text size="sm">For a panel with a heading and a paragraph in it.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="open" padding="xl">
    <x-shape::text size="sm">For a panel that is a small page of its own.</x-shape::text>
</x-shape::popover>
