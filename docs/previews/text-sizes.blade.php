<x-shape::text size="xl">We'll only use this for receipts.</x-shape::text>
<x-shape::text size="lg">We'll only use this for receipts.</x-shape::text>
<x-shape::text size="base">We'll only use this for receipts.</x-shape::text>
<x-shape::text size="sm">We'll only use this for receipts.</x-shape::text>
<x-shape::text size="xs">We'll only use this for receipts.</x-shape::text>
