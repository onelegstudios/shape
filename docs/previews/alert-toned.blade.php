<x-shape::alert tone="danger" variant="outline" heading="Card declined">
    Update the card on file. The border and the glyph carry the tone; the text is
    the page's own ink.
</x-shape::alert>

<x-shape::alert tone="danger" variant="outline" heading="Card declined" toned>
    Update the card on file. Toned, the whole block reads in the tone.
</x-shape::alert>

<x-shape::alert tone="danger" variant="ghost" heading="Card declined" toned>
    Update the card on file. Ghost is untoned too — until you point at it, where
    it paints the tint and takes the ink that belongs on it.
</x-shape::alert>
