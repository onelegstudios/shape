<x-shape::popover.trigger for="start-edge" variant="subtle">bottom-start</x-shape::popover.trigger>
<x-shape::popover.trigger for="end-edge" variant="subtle">bottom-end</x-shape::popover.trigger>
<x-shape::popover.trigger for="over" variant="subtle">top</x-shape::popover.trigger>

<x-shape::popover name="start-edge" placement="bottom-start">
    <x-shape::text size="sm">Aligned to the trigger's start edge.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="end-edge" placement="bottom-end">
    <x-shape::text size="sm">Aligned to its end edge.</x-shape::text>
</x-shape::popover>

<x-shape::popover name="over" placement="top">
    <x-shape::text size="sm">Above it, centred.</x-shape::text>
</x-shape::popover>
